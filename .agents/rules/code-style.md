---
trigger: always_on
---

Use `pnpm run` commands:

- Check formatting: `pnpm run lint`
- Fix formatting: `pnpm run lint:fix`

## Versioning

- Always start new projects at version `0.0.0`.

> We use Prettier and TypeScript.
